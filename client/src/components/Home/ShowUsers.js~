import React from "react"

export default function ShowUsers ({ allusers }) {

	console.log(`showuser ${allusers}`)

	return (
		<div>
			{allusers.map((user) => `${user.id} ${user.username}`)}
		</div>
	)
}
